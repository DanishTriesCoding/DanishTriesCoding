# 1. Set your exact project paths
export ARR_ROOT=/home/cs/Desktop/Evolving-Agents-Danish
export INTEGRATION=$ARR_ROOT/AgentEvolver-integration
export AE_ROOT=$ARR_ROOT/Frameworks/AgentEvolver
export PYTHON=$(which python)
export MODEL=Qwen/Qwen2.5-7B-Instruct

# 2. Navigate to the data processing folder
cd $ARR_ROOT/Data_Processing

# 3. Generate the Urdu translated variant for the DROP benchmark
python code_switch.py drop translate ur --limit 100

# 4. Navigate to the integration folder
cd $INTEGRATION

# 5. Compile everything into the final task files needed for training
bash scripts/prepare_all_tasks.sh
